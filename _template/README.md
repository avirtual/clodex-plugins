# my-plugin

What it does, in two sentences.

**Seat**: which seat should hold it and why.

**Writes**: where on disk it keeps state (plugin state lives under the app's
`userData/plugins/<id>/`, via `host.storage`; anything else, say so).
