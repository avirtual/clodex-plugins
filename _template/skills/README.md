Drop `<name>/SKILL.md` folders here. A seat that holds the plugin sees them as
`/<plugin-id>:<name>`; `agents/<name>.md`, `prompts/system|append/<stem>.md` and
`templates/<stem>.json` work the same way. Delete this folder if the plugin
ships no skills.
